{
  "name": "@silo/cli",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "license": "MIT",
  "bin": {
    "silo": "./dist/main.js"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "start": "tsx src/main.ts"
  },
  "devDependencies": {
    "@silo/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
