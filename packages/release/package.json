{
  "name": "@silo/release",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run --no-file-parallelism",
    "release": "tsx src/run.ts"
  },
  "devDependencies": {
    "@silo/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
