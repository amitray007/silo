import { ComingSoon } from '../components';

/** `/trash` — restore/delete-now/empty land in a later slice. */
export function TrashView() {
  return <ComingSoon title="Trash — coming soon" subtitle="Deleted links will appear here." />;
}
