import { ComingSoon } from '../components';

/** `/` — the Library list view lands in the next slice. */
export function LibraryView() {
  return <ComingSoon title="Library — coming soon" subtitle="Your kept links will appear here." />;
}
