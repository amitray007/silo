/**
 * Production build stub for `agentation` (see vite.config.ts). The dev-only
 * annotation toolbar is aliased to this empty module in `vite build`, so the
 * real (dev-only) library never enters the production bundle. `DevTools` guards
 * the dynamic import behind `import.meta.env.DEV`, so this stub is never
 * actually rendered — it exists only to give the aliased import something
 * trivial to resolve to.
 */
export function Agentation(): null {
  return null;
}
